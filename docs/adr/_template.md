1. **Title**
   {{ 결정 제목 }}

2. **Status**
   {{ `채택`, `대체됨`, `ADR NNN 대체` 중 하나를 선택 }}

3. **Context**
   {{ 이 결정을 둘러싼 배경, 제약, 문제 상황 }}

4. **Decision**
   {{ 무엇을 선택했는지, 왜 그 선택을 했는지, 어떤 대안을 기각했는지 }}

5. **Consequences**
   {{ 이 결정의 예상 영향. 좋은 점과 나쁜 점 둘 다 적어서, 트레이드오프가 드러나게 함. }}
